import {LightingModeDirective} from './lighting-mode.directive';

describe('LightingModeDirective', () => {
  it('should create an instance', () => {
    const directive = new LightingModeDirective();
    expect(directive).toBeTruthy();
  });
});
